[project]
name = "minicodex"
version = "0.2.0"
description = "A small coding agent implemented without agent frameworks."
requires-python = ">=3.10"
dependencies = []

[project.scripts]
minicodex = "minicodex.cli:main"

[tool.setuptools]
packages = ["minicodex"]

